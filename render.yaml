services:
  - name: solana-auth-server
    type: web
    runtime: python
    buildCommand: pip install -r requirements.txt
    startCommand: gunicorn server:app
    envVars:
      - key: FIREBASE_CREDENTIALS
        sync: false
